<Window x:Class="Aplicacion_Ejemplo.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window1" Height="620" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F7">

    <Grid>

        <!-- ====== FILAS ====== -->
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <!-- Encabezado más alto para evitar que tape -->
            <RowDefinition Height="*"/>
            <!-- Tarjeta central -->
        </Grid.RowDefinitions>

        <!-- ====== ENCABEZADO ====== -->
        <StackPanel Grid.Row="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="780"
                    Margin="0,10,0,0">

            <TextBlock Text="MISALA UDD"
                       Foreground="#2B5FA8"
                       FontSize="18"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0,0,0,4"/>

            <TextBlock x:Name="txtBienvenida"
                       Text="Bienvenido"
                       Foreground="#2B2F36"
                       FontSize="20"
                       FontWeight="SemiBold"
                       TextAlignment="Center"
                       Margin="0,0,0,4"/>

            <TextBlock x:Name="txtTipoUsuario"
                       Text=""
                       Foreground="#4A4F57"
                       FontSize="15"
                       TextAlignment="Center"
                       Margin="0,0,0,0"/>
        </StackPanel>

        <!-- ====== TARJETA ====== -->
        <Grid Grid.Row="1" HorizontalAlignment="Center">

            <Border Background="White"
                    CornerRadius="10"
                    BorderBrush="#E6E9EE"
                    BorderThickness="1"
                    Padding="20"
                    Width="520"
                    Height="auto">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- TITULOS -->
                    <TextBlock Grid.Row="0"
                               Text="Acceso Rápido"
                               FontSize="15"
                               FontWeight="Bold"
                               Foreground="#2B2F36"
                               Margin="0,0,0,6"/>

                    <TextBlock Grid.Row="1"
                               Text="Accede a las funciones principales"
                               FontSize="12"
                               Foreground="#7B8086"
                               Margin="0,0,0,15"/>

                    <!-- BOTÓN INICIO -->
                    <Button x:Name="btn_inicio"
                            Grid.Row="3"
                            Height="40"
                            Margin="0,0,0,10"
                            Background="Transparent"
                            Click="btn_inicio_Click"
                            BorderThickness="0">
                        <Border Background="#133B8A" CornerRadius="6" Padding="10" Width="468">
                            <TextBlock Text="Inicio"
                                       Foreground="White"
                                       FontSize="14"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </Button>

                    <!-- BOTÓN VER EDIFICIOS -->
                    <Button x:Name="btn_edificios"
                            Grid.Row="4"
                            Height="40"
                            Margin="0,0,0,10"
                            Background="Transparent"
                            Click="btn_edificios_Click"
                            BorderThickness="0">
                        <Border Background="#133B8A" CornerRadius="6" Padding="10" Width="468">
                            <TextBlock Text="Ver Edificios"
                                       Foreground="White"
                                       FontSize="14"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </Button>

                    <!-- BOTÓN RESERVAS -->
                    <Button x:Name="btn_reservas"
                            Grid.Row="5"
                            Height="40"
                            Margin="0,0,0,10"
                            Background="Transparent"
                            Click="btn_reservas_Click"
                            BorderThickness="0">
                        <Border Background="#133B8A" CornerRadius="6" Padding="10" Width="468">
                            <TextBlock Text="Gestionar Reservas"
                                       Foreground="White"
                                       FontSize="14"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </Button>

                    <!-- BOTÓN PERFIL -->
                    <Button x:Name="btn_perfil"
                            Grid.Row="6"
                            Height="40"
                            Margin="0,0,0,10"
                            Background="Transparent"
                            Click="btn_perfil_Click"
                            BorderThickness="0">
                        <Border Background="#133B8A" CornerRadius="6" Padding="10" Width="468">
                            <TextBlock Text="Mi Perfil"
                                       Foreground="White"
                                       FontSize="14"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </Button>

                    <!-- BOTÓN LOGOUT -->
                    <Button x:Name="btn_logout"
                            Grid.Row="7"
                            Height="40"
                            Background="Transparent"
                            Click="btn_logout_Click"
                            BorderThickness="0">
                        <Border Background="White" CornerRadius="6"
                                BorderBrush="#F6C6C6" BorderThickness="1.5"
                                Padding="10" Width="468">
                            <TextBlock Text="Cerrar Sesión"
                                       Foreground="#D24B4B"
                                       FontSize="14"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </Button>

                </Grid>
            </Border>

        </Grid>

    </Grid>
</Window>
