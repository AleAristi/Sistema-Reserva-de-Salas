<Window x:Class="Aplicacion_Ejemplo.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window2" Height="620" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F7">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <!-- barra superior -->
            <RowDefinition Height="Auto"/>
            <!-- encabezado -->
            <RowDefinition Height="*"/>
            <!-- contenido -->
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Grid Grid.Row="0" Background="#163C8F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="MISALA UDD"
                       Foreground="White"
                       VerticalAlignment="Center"
                       FontSize="14"
                       FontWeight="Bold"
                       Margin="12,0"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Grid.Column="1"
						Margin="10,0,0,0">

                <Button x:Name="btn_inicio"
                        Content="Inicio"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Width="65"
                        Cursor="Hand"
                        Click="btn_inicio_Click"/>

                <Button x:Name="btn_edificio"
                        Content="Edificios"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Margin="20,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Width="41"
                        Cursor="Hand"
                        Click="btn_edificio_Click"/>

                <Button x:Name="btn_reserva"
                        Content="Reservas"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Margin="20,0"
                        VerticalAlignment="Center"
                        Cursor="Hand"
                        Click="btn_reserva_Click"/>

                <Button x:Name="btn_perfil"
                        Content="Perfil"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Margin="20,0"
                        VerticalAlignment="Center"
                        Cursor="Hand"
                        Click="btn_perfil_Click"/>

            </StackPanel>

            <Button x:Name="btn_logout"
                    Content="Salir"
                    Foreground="White"
                    Background="Transparent"
                    BorderThickness="0"
                    Margin="24,0,60,0"
                    VerticalAlignment="Center"
                    Cursor="Hand"
                    Grid.Column="2"
                    Click="btn_logout_Click"/>
        </Grid>

        <!-- Encabezado -->
        <StackPanel Grid.Row="1"
                    Margin="40,16,40,0">
            <TextBlock Text="Edificios"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="#2B2F36"
                       Margin="0,4,0,2"/>
            <TextBlock Text="Edificios disponibles en la Universidad del Desarrollo"
                       FontSize="12"
                       Foreground="#7B8086"/>
        </StackPanel>

        <!-- Tarjeta Edificios (CORREGIDA Y ALINEADA) -->
        <Grid Grid.Row="2" Margin="40,20,40,20">
            <Border Background="White"
                    CornerRadius="6"
                    BorderBrush="#E6E9EE"
                    BorderThickness="1"
                    Width="520"
                    HorizontalAlignment="Left">

                <StackPanel>

                    <ListBox x:Name="listEdificios"
             FontSize="18"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>

</Window>