<Window x:Class="Aplicacion_Ejemplo.Window3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mi Perfil" Height="620" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F7">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <!-- Barra superior -->
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Contenido -->
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Grid Grid.Row="0" Background="#163C8F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="MISALA UDD"
                       Foreground="White"
                       VerticalAlignment="Center"
                       FontSize="15"
                       FontWeight="Bold"
                       Margin="16,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"
                        VerticalAlignment="Center" Grid.Column="1" Margin="10,0,0,0">
                <Button x:Name="btn_inicio" Content="Inicio" Foreground="White"
                        Background="Transparent" BorderThickness="0" Width="65"
                        Cursor="Hand" Click="btn_inicio_Click"/>
                <Button x:Name="btn_edificio" Content="Edificios" Foreground="White"
                        Background="Transparent" BorderThickness="0" Margin="20,0"
                        FontWeight="SemiBold" Width="41" Cursor="Hand" Click="btn_edificio_Click"/>
                <Button x:Name="btn_reserva" Content="Reservas" Foreground="White"
                        Background="Transparent" BorderThickness="0" Margin="20,0"
                        Cursor="Hand" Click="btn_reserva_Click"/>
                <Button x:Name="btn_perfil" Content="Perfil" Foreground="White"
                        Background="Transparent" BorderThickness="0" Margin="20,0"
                        Cursor="Hand" Click="btn_perfil_Click"/>
            </StackPanel>

            <Button x:Name="btn_logout" Content="Salir" Foreground="White"
                    Background="Transparent" BorderThickness="0"
                    Margin="24,0,60,0" VerticalAlignment="Center"
                    Cursor="Hand" Grid.Column="2" Click="btn_logout_Click"/>
        </Grid>

        <!-- Encabezado -->
        <StackPanel Grid.Row="2" Margin="140,53,-20,385">
            <TextBlock Text="Mi Perfil" FontSize="18" FontWeight="Bold"
                       Foreground="#2B2F36" Margin="0,0,0,4"/>
            <TextBlock Text="Información de tu cuenta en MISALA UDD" FontSize="12"
                       Foreground="#7B8086"/>
        </StackPanel>

        <!-- Contenido -->
        <Grid Grid.Row="2" Margin="60,24,60,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <!-- Tarjeta foto -->
                <ColumnDefinition Width="24"/>
                <!-- Separador -->
                <ColumnDefinition Width="*"/>
                <!-- Tarjeta info -->
            </Grid.ColumnDefinitions>

            <!-- Tarjeta INFORMACIÓN -->
            <Border Background="White" CornerRadius="8" BorderBrush="#E6E9EE"
                    BorderThickness="1" Padding="20" VerticalAlignment="Center"
                    MinHeight="230" MaxWidth="720" Grid.ColumnSpan="3"
                    Margin="142,0,142,0" Height="326">
                <StackPanel>
                    <TextBlock Text="Información Personal" FontSize="14"
                               FontWeight="Bold" Foreground="#2B2F36" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Nombre -->
                        <TextBlock Text="Nombre Completo" FontSize="11"
                                   Foreground="#7B8086" Grid.Row="0" Grid.Column="0"/>
                        <TextBlock Text="{Binding Nombre}" FontSize="13"
                                   Foreground="#333" Grid.Row="0" Grid.Column="1"/>

                        <!-- Carrera -->
                        <TextBlock Text="Carrera" FontSize="11" Foreground="#7B8086"
                                   Grid.Row="1" Grid.Column="0" Margin="0,14,0,0"/>
                        <TextBlock Text="{Binding Carrera}" FontSize="13"
                                   Foreground="#333" Grid.Row="1" Grid.Column="1" Margin="0,14,0,0"/>

                        <!-- Correo/Usuario -->
                        <TextBlock Text="Correo Institucional" FontSize="11"
                                   Foreground="#7B8086" Grid.Row="2" Grid.Column="0"
                                   Margin="0,14,0,0"/>
                        <TextBlock Text="{Binding Usuario}" FontSize="13"
                                   Foreground="#333" Grid.Row="2" Grid.Column="1" Margin="0,14,0,0"/>

                        <!-- Tipo de usuario -->
                        <TextBlock Text="Tipo de Usuario" FontSize="11"
                                   Foreground="#7B8086" Grid.Row="3" Grid.Column="0"
                                   Margin="0,14,0,0"/>
                        <TextBlock Text="{Binding Tipo}" FontSize="13"
                                   Foreground="#333" Grid.Row="3" Grid.Column="1" Margin="0,14,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
